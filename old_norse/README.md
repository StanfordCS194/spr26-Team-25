# Old Norse Data

Vocabulary and examples data derived from public-domain sources:
- Zoëga Old Norse dictionary (via CLTK)
- Heimskringla corpus (via CLTK)

The pipeline that generated these files (`pipeline/old_norse/`) was written by Neil — see the `neil-test` branch for the full implementation.